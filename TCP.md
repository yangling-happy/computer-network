# TCP报文首部标志位

目前，TCP协议使用的标志位为9位（教材中多为6位），其含义分别如下：

- NS—ECN-nonce，允许拥塞控制的端对端通知而避免丢包（用显示标注代替丢包来提示网络拥塞）；
- CWR—Congestion Window Reduced，通知对端减小拥塞窗口；
- ECE—ECN-Echo有两种意思，取决于SYN标志的值；
- URG—为1表示高优先级数据包，此时紧急指针字段有效；
- ACK—为1表示确认号字段有效；
- PSH—为1表示是带有PUSH标志的数据，指示接收方应该尽快将这个报文段交给应用层而不用等待缓冲区装满；
- RST—为1表示出现严重差错，此时可能需要重新建立TCP连接。还可以用于拒绝非法的报文段和拒绝连接请求；
- SYN—为1表示这是连接请求或是连接接受请求，用于建立连接和使顺序号同步；
- FIN—为1表示发送方没有数据要传输了，要求释放连接。

# 三握四挥
<img width="1050" height="710" alt="image" src="https://github.com/user-attachments/assets/1806712f-0752-47b7-bab5-bdf42fcf629b" />
<img width="1043" height="737" alt="image" src="https://github.com/user-attachments/assets/199bb71a-44a0-4cb9-b98a-bba5b80c6848" />

# 传统的TCP拥塞控制

就是一套**TCP协议自己制定的“交通规则”**，目的是避免在网络“堵车”时，大家因为都拼命发数据，反而导致谁的数据都送不到。

想象你开车上高速，要经过一个收费站。

-   **网络带宽** = 高速公路的**车道数量**（比如4车道）。
-   **你的数据** = 你的**车**。
-   **TCP拥塞控制** = 收费站的**智能放行系统**。

这个系统的目标只有一个：**在不引起高速公路大堵车的前提下，尽快把你所有的车（数据）都送出去。**

### 传统TCP拥塞控制的四大核心“战术”

传统的TCP拥塞控制（主要指TCP Tahoe 和 TCP Reno 这两个经典版本）主要有四个核心步骤：

1.  **慢启动：新车队先试探路况**
    -   **做法**：刚开始，你不知道路况如何。先放1辆车出去，收到“已到达”的确认后，加倍放行（2辆、4辆、8辆……）。数据发送速度呈**指数级增长**。
    -   **目的**：快速摸清网络的承载能力。

2.  **拥塞避免：摸着石头过河**
    -   **做法**：当车速（发送速率）达到一个安全阈值后，就不再野蛮加速了。改为每次只增加1辆车（线性增长），小心翼翼地试探网络的极限。
    -   **目的**：在保证不拥堵的前提下，缓慢提升速度。

3.  **快速重传：发现有车掉队，立刻重发**
    -   **场景**：你连续发了5辆车，但第2辆车迟迟没收到“已到达”的回执。收到3次同样的提醒（重复确认）后，TCP会认为“糟糕，第2辆车很可能丢了”。
    -   **做法**：不等超时，**立刻重发**那辆丢失的车。
    -   **目的**：快速修复小问题，避免因为等待超时浪费时间。

4.  **快速恢复：亡羊补牢，继续前行**
    -   **场景**：在快速重传之后，网络可能只是轻微拥塞，还没完全瘫痪。
    -   **做法**：不把速度降到0（不重新慢启动），而是把发送速度减半，然后继续以“拥塞避免”模式慢慢增加。
    -   **目的**：在发生小事故后，既能缓解拥堵，又能维持较高的通行效率。

### 一个直观的“发送速率”变化图

你可以把这个过程想象成过山车：

1.  **起步**（慢启动）：速度从0直线飙升。
2.  **撞墙**（发生丢包）：速度瞬间**腰斩**（减半）或归零。
3.  **再加速**（拥塞避免）：速度缓慢、线性回升。
4.  **循环**：不断重复“撞墙-减速-再加速”的过程。

```text
发送速率
   ↑
   |    /‾‾‾‾‾‾‾‾‾‾‾‾\
   |   /             \   (拥塞避免：线性增长)
   |  /               \
   | /                 \_______ (发生丢包，速率减半)
   |/                          \
   +--------------------------------→ 时间
   慢启动   →   拥塞避免   →  丢包   →  重复...
```

<img width="927" height="621" alt="image" src="https://github.com/user-attachments/assets/e61acc9d-fedc-48e5-b6dc-332b1eef2e32" />


### 为什么需要它？

如果没有拥塞控制，所有设备都像疯子一样同时往网络里发数据（），结果就是：

1.  **路由器缓存爆满**，只能大量丢弃数据包（**丢包**）。
2.  **重传加剧拥堵**，丢包后大家重传，让网络更堵。
3.  **最终网络瘫痪**，几乎无法通信，这叫**拥塞崩溃**。

**拥塞控制的核心思想就是：“在网络即将堵死之前，自己主动减速。”** 

这种“牺牲小我，成就大我”的协作机制，是互联网能稳定运行的基石。

### 总结

| 术语 | 比喻理解 | 一句话解释 |
| :--- | :--- | :--- |
| **慢启动** | 新车队先试探性加速 | 指数级增长速度，快速探测网络容量 |
| **拥塞避免** | 摸石头过河，谨慎提速 | 线性增长速度，避免突然堵车 |
| **快速重传** | 发现丢车立刻重发 | 不等待超时，快速修复丢包 |
| **快速恢复** | 小事故后，半速继续开 | 速度减半但不归零，保持效率 |

### 重要提示：“传统”二字的意义

“传统TCP拥塞控制”，通常指的就是上面这套机制（主要来自**Reno**和**CUBIC**等经典算法）。

但请注意，这套“交通规则”是**20多年前**设计的。

如今的高速公路（网络）已经变成了：

-   **高带宽**（100M -> 1G -> 10G...）
-   **低延迟**（光纤）
-   **复杂路况**（移动网络、卫星网络）

传统算法在“高带宽长距离”的网络环境下，就像在德国无限速高速上还按60km/h龟速行驶，**效率低下**。

因此，现在有了更新的算法，比如**BBR**（谷歌提出，类似“智能巡航模式”，通过测量实际“带宽”和“延迟”来动态调整速度，而不是等丢包）。

所以，“传统的TCP拥塞控制”可以理解为：**那种主要靠“丢包”来作为减速信号，采用“加性增乘性减”策略的经典拥塞控制方法。**
