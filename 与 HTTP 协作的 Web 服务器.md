# 用单台虚拟主机实现多个域名

一个服务器里面可以搭建多个web站点，虚拟主机可以假想有多台服务器。

域名通过DNS 服务映射到IP 地址 （域名解析）之后访问。

如果一台服务器内托管了 www.tricorder.jp 和 www.hackr.jp 这两个域名，当收到请求时就需要弄清楚究竟要访问哪个域名。

# 通信数据转发程序 ：代理、网关、隧道

- 代理

转发请求和响应。

每次通过代理服务器转发请求或响应时，会追加写入 **Via** 首部信息

①缓存代理

将资源的副本缓存保存在代理服务器上面

②透明代理（对应非透明代理）

不对报文做任何加工的代理类型（那之前的缓存代理算不算？）



回答： 

**缓存代理和透明代理不是互斥的概念，它们是按不同维度对代理进行的分类。**

- 按功能分：有缓存代理 和 非缓存代理。
- 按工作方式分：有透明代理和非透明代理（显式代理）。
  - **透明代理**：指客户端不需要在浏览器或应用中做任何代理设置，感觉不到代理的存在。代理服务器在后台透明地拦截并处理网络请求。
  - **非透明代理（显式代理）**：需要客户端手动配置代理服务器的地址和端口。

所以，**一个代理服务器可以同时是“缓存代理”和“透明代理”**。例如，一个公司可以在网关上部署Squid，并将其配置为透明模式，同时开启缓存功能。这样，员工上网无需任何设置，而Squid可以缓存常用的网页资源，既实现了“透明”拦截，又提供了“缓存”加速。

- 网关

网关是转发其他服务器通信数据的服务器，接收从客户端发送来的请求时，它就像自己拥有资源的源服务器一样对请求进行处理。有时客户端可能都不会察觉，自己的通信目标是一个网关。（蛤？）

回答：

这里的“网关”指的不是我们通常说的网络层网关（如路由器），而是应用层的**反向代理网关**。它的工作原理是：

1. **接收请求**：客户端（如你的浏览器）向`www.example.com`发送请求，DNS将域名解析到**网关服务器**的IP。所以，从客户端的视角看，它以为自己在直接和`www.example.com`这个源服务器通信。
2. **转发请求**：网关收到请求后，会根据配置（如请求的路径`/api`），将请求**转发**给内部网络中的真实服务器（比如一台专门处理API的服务器，IP可能是192.168.1.10）。
3. **接收响应并返回**：内部服务器处理完请求，将结果返回给网关，网关再把这个结果返回给客户端。

**在整个过程中，客户端只知道网关的存在，对网关背后的真实服务器一无所知。因此，你会觉得它“就像自己拥有资源的源服务器一样”。**

可以在客户端和网关之间的通信线路上面确保连接的数据安全。

- 隧道

隧道是在相隔甚远的客户端和服务器两者之间进行中转，并保持双方通信连接的应用程序。也可用ssl加密提高安全性，透明的，不会影响请求。

# 保存资源的缓存

缓存服务器的优势在于利用缓存可避免多次从源服务器转发资源。因此客户端可就近从缓存服务器上获取资源，而源服务器也不必多次处理相同的请求了。

- 缓存有效期限

即使存在缓存，也会因为客户端的要求、缓存的有效期等因素，向源服务器确认资源的有效性。若判断缓存失效，缓存服务器将会再次从源服务器上获取“新”资源。
